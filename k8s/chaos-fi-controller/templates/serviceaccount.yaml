---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $.Chart.Name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ $.Chart.Name }}
    chart_name: {{ $.Chart.Name }}
    chart_version: {{ $.Chart.Version }}
    service: {{ $.Chart.Name }}
    team: {{ $.Values.team }}
