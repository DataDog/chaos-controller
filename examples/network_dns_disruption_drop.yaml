apiVersion: chaos.datadoghq.com/v1beta1
kind: Disruption
metadata:
  name: dns-timeout
  namespace: chaos-demo
spec:
  level: pod
  selector:
    app: demo-curl
  count: 1
  duration: 5m
  dnsDisruption:
    domains:
      - example.com
    failureMode: drop
    protocol: both
