
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: networkfailureinjections.chaos.datadoghq.com
spec:
  group: chaos.datadoghq.com
  names:
    kind: NetworkFailureInjection
    listKind: NetworkFailureInjectionList
    plural: networkfailureinjections
    shortNames:
    - nfi
    singular: networkfailureinjection
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: NetworkFailureInjection is the Schema for the networkfailureinjections
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: NetworkFailureInjectionSpec defines the desired state of NetworkFailureInjection
          properties:
            failure:
              description: NetworkFailureInjectionSpecFailure defines the failure
                spec
              properties:
                host:
                  type: string
                port:
                  type: integer
                probability:
                  type: integer
                protocol:
                  type: string
              required:
              - port
              - probability
              - protocol
              type: object
            numPodsToTarget:
              description: 'Note: the +optional should eventually be generated by
                the scaffolding, see https://github.com/kubernetes-sigs/kubebuilder/issues/479'
              minimum: 0
              type: integer
            selector:
              additionalProperties:
                type: string
              description: Set is a map of label:value. It implements Labels.
              type: object
          required:
          - failure
          - selector
          type: object
        status:
          description: NetworkFailureInjectionStatus defines the observed state of
            NetworkFailureInjection
          properties:
            finalizing:
              type: boolean
            injected:
              type: boolean
            pods:
              items:
                type: string
              type: array
          required:
          - finalizing
          - injected
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
