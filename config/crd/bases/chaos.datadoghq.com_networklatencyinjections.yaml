
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: networklatencyinjections.chaos.datadoghq.com
spec:
  group: chaos.datadoghq.com
  names:
    kind: NetworkLatencyInjection
    listKind: NetworkLatencyInjectionList
    plural: networklatencyinjections
    shortNames:
    - nli
    singular: networklatencyinjection
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: NetworkLatencyInjection is the Schema for the networklatencyinjections
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: NetworkLatencyInjectionSpec defines the desired state of NetworkLatencyInjection
          properties:
            count:
              type: integer
            delay:
              type: integer
            hosts:
              items:
                type: string
              type: array
            selector:
              additionalProperties:
                type: string
              description: Set is a map of label:value. It implements Labels.
              type: object
          required:
          - count
          - delay
          - hosts
          - selector
          type: object
        status:
          description: NetworkLatencyInjectionStatus defines the observed state of
            NetworkLatencyInjection
          properties:
            finalizing:
              type: boolean
            injected:
              type: boolean
            pods:
              items:
                type: string
              type: array
          required:
          - finalizing
          - injected
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
